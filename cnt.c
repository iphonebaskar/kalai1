#include <stdio.h>
void main()
{
    long long a;
    int count = 0;

    printf("Enter an integer: ");
    scanf("%lld", &a);

    while(n != 0)
    {
        // a = a/10
        na/= 10;
        ++count;
    }

    printf("Number of digits: %d", count);
    getch();
}
